<?xml version="1.0" encoding="utf-8"?>
<!--
  Attached only by the debug and profile manifests, never by the release one.
  A release build has no network security config, which on targetSdk 28+ means
  cleartext HTTP is denied outright — production talks HTTPS to
  api.meuauto.com.br and nothing else.

  The exceptions below are the two addresses a development build needs: the
  Android emulator's alias for the host loopback, and localhost.

  To test on a physical device, add the host machine's LAN IPv4 as another
  <domain> and open TCP port 8080 in Windows Firewall (see docs/RODANDO.md).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
